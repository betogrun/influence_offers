<nav class="bg-blue-500 p-4 text-white">
  <div class="container mx-auto flex justify-between items-center">
    <%= link_to "INFLUENCE OFFERS", root_path, class: "text-lg lg:text-xl font-bold" %>
    <div class="flex items-center">
      <% if player_signed_in? %>
        <%= button_to "Offers", offers_path, method: :get, class: "px-4 py-2 bg-blue-400 hover:bg-blue-600 text-white font-bold rounded mx-2 text-sm lg:text-base" %>
        <%= button_to "Claimed Offers", claimed_offers_path, method: :get, class: "px-4 py-2 bg-blue-400 hover:bg-blue-600 text-white font-bold rounded mx-2 text-sm lg:text-base" %>
        <%= button_to "Logout", destroy_player_session_path, method: :delete, form: { "data-turbo" => "false" }, class: "px-4 py-2 bg-red-500 hover:bg-red-700 text-white font-bold rounded mx-2 text-sm lg:text-base" %>
      <% end %>
    </div>
  </div>
</nav>